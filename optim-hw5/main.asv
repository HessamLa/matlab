rangeX = [ 0 20];
rangeY = [-5 15];
n = 20;
xy = generate_points(n, rangeX, rangeY);

proximity = calculate_distance(xy);

% generate random order of the 21 cities, first and last must be the same



